# MCP Server Configuration
MCP_SERVER_NAME=mcp-server-starter
MCP_SERVER_VERSION=1.0.0

# Transport Configuration
TRANSPORT_TYPE=http
HTTP_HOST=0.0.0.0
HTTP_PORT=3000

# ============================================================================
# REQUIRED: Public URL
# The URL where this server is accessible (used for OAuth callbacks and metadata)
# For local development: http://localhost:3000
# For production: your actual URL (e.g., https://mcp.example.com)
# ============================================================================
PUBLIC_URL=http://localhost:3000

# OAuth Configuration (optional - uncomment to enable)
#AUTH_ENABLED=true
#OAUTH_ISSUER_URL=https://your-oauth-provider.com

# Service Documentation URL (optional - link to your MCP server docs)
#SERVICE_DOCUMENTATION_URL=https://github.com/your-org/your-mcp-server

# IP Filtering (optional, comma-separated IPs or CIDR ranges)
#FILTER_BY_IP=127.0.0.1,192.168.1.0/24

# Email Filtering (optional, comma-separated emails)
# When set, only these emails can access the MCP server after OAuth authentication
#ALLOWED_EMAILS=test@example.com,admin@company.org

# Request Logging (optional, defaults to true)
# Set to false to disable HTTP request/response logging
#REQUEST_LOGGING=false