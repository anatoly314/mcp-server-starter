# MCP Server Configuration
MCP_SERVER_NAME=mcp-server-sandbox
MCP_SERVER_VERSION=1.0.0

# Transport Configuration - STDIO Mode
TRANSPORT_TYPE=stdio

# Note: Most other settings are not used in stdio mode
# since communication happens via stdin/stdout